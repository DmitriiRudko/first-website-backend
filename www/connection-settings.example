<?php

define(HOST, ' ');
define(DB, ' ');
define(USER, ' ');
define(PASSWD, ' ');
define(CHARSET, 'utf8');

define(DNS, "mysql:host= ;dbname= ;charset=utf8");
define(OPT, [
    PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
    PDO::ATTR_EMULATE_PREPARES => false,
]);
